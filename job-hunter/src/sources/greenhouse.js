import { fetchJson } from '../util.js';
export const metadata = { source:'greenhouse', access_type:'public_api', supports_incremental:true, supports_salary:true, terms_notes:'Official Greenhouse Job Board API' };
export function adaptGreenhouse(payload, company) { return (payload.jobs || []).map((j) => ({ source:'greenhouse', source_external_id:String(j.id), source_url:j.absolute_url, canonical_url:j.absolute_url, apply_url:j.absolute_url, company:company.name, domain:company.domain, title:j.title, description:j.content || '', location:j.location?.name || '', salary:j.pay_input_ranges?.length ? j.pay_input_ranges.map((r)=>`${r.title||'base'} $${r.min_cents/100}–$${r.max_cents/100}`).join('; ') : null, published_at:j.first_published || j.updated_at || null, raw:j })); }
export async function fetchGreenhouse(company) { return adaptGreenhouse(await fetchJson(`https://boards-api.greenhouse.io/v1/boards/${encodeURIComponent(company.greenhouse_token)}/jobs?content=true&pay_transparency=true`), company); }
