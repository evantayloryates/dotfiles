const REMOVABLE_SYSTEM=new Set(['INBOX','IMPORTANT','CATEGORY_FORUMS','CATEGORY_UPDATES','CATEGORY_PERSONAL','CATEGORY_PROMOTIONS','CATEGORY_SOCIAL','YELLOW_STAR','STARRED']);
export function parseLabels(text){const sections={system:[],user:[]};let type=null,current={};for(const line of text.split(/\r?\n/)){if(line.startsWith('System Labels:'))type='system';else if(line.startsWith('User Labels:'))type='user';else if(line.startsWith('ID: '))current={id:line.slice(4).trim()};else if(line.startsWith('Name: ')&&current.id&&type){current.name=line.slice(6).trim();sections[type].push(current);current={};}}return sections;}
export function labelPlan(text,queueName='Ingest::Jobs'){const labels=parseLabels(text);const queue=labels.user.find(l=>l.name===queueName);if(!queue)throw new Error(`Required Gmail label ${queueName} was not found`);const cleanup=[...labels.user.filter(l=>l.id!==queue.id).map(l=>l.id),...labels.system.filter(l=>REMOVABLE_SYSTEM.has(l.id)).map(l=>l.id)];return {queueLabelId:queue.id,cleanupLabelIds:cleanup};}
