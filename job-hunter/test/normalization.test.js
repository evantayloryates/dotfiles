import test from 'node:test';import assert from 'node:assert/strict';import { parseSalary } from '../src/salary.js';import { normalizeUrl,normalizeDomain,normalizeCompanyName } from '../src/normalize.js';import { normalizeLocation } from '../src/locations.js';
test('salary parser separates base, total compensation, and hourly rates',()=>{assert.deepEqual(parseSalary('$220K-$280K base').base_max,280000);assert.equal(parseSalary('$300K total comp').base_max,null);assert.equal(parseSalary('$300K total comp').total_comp_max,300000);assert.equal(parseSalary('$95/hour').period,'hour');assert.equal(parseSalary('$95/hour').base_max,null);});
test('URL and company identity normalization is deterministic',()=>{assert.equal(normalizeUrl('https://example.com/jobs/1?utm_source=x&keep=yes'),'https://example.com/jobs/1?keep=yes');assert.equal(normalizeDomain('https://www.Example.com/jobs'),'example.com');assert.equal(normalizeCompanyName(' Example, Inc. '),'example inc');});
test('LinkedIn job URLs discard private alert tracking parameters',()=>{assert.equal(normalizeUrl('https://www.linkedin.com/comm/jobs/view/123?otpToken=secret&midSig=private'),'https://www.linkedin.com/comm/jobs/view/123');});
test('location preference facts are structured',()=>{assert.deepEqual(normalizeLocation('New York, NY (Hybrid)'),{display:'New York, NY (Hybrid)',city:'New York',region:'NY',country:'US',workplace_type:'hybrid'});assert.equal(normalizeLocation('United States Remote').workplace_type,'remote');});
