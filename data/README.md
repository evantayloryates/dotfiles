# data/

General-purpose lightweight file store for machine-local runtime data.

Contents are git-ignored unless explicitly carved out in `.gitignore`.
Consumers must be idempotent and self-seed if this directory or their subtree is missing.
