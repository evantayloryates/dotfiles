#!/usr/bin/python3
"""Route ordinary op invocations through the dotfiles singleton broker."""
import sys
from pathlib import Path
sys.dont_write_bytecode = True
sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "src/onepassword"))
from op_agent import client
sys.exit(client(sys.argv[1:]))
